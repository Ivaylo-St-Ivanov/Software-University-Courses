function stringLength(param1, param2, param3) {

    let sumOfTheLength = param1.length + param2.length + param3.length;

    console.log(sumOfTheLength);
    console.log(Math.floor(sumOfTheLength / 3));
}
stringLength('chocolate', 'ice cream', 'cake');